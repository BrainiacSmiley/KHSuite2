<ul class="nav secondary-nav">
  <li><%= link_to t('link_signout'), signout_path, :method => :delete %></li>
</ul>
<% if current_user.admin? %>
<ul class="nav secondary-nav">
  <li class="dropdown">
    <a href="#" class="dropdown-toggle">Admin</a>
    <ul class="dropdown-menu">
      <%= topbar_active_list(@title, t(:title_user_all)) %><%= link_to t('link_userlist'), users_path %></li>
      <%= topbar_active_list(@title, t(:title_doctor_all)) %><%= link_to t('link_doctorlist'), doctors_path %></li>
    </ul>
  </li>
</ul>
<% end %>
<ul class="nav secondary-nav">
  <% if @user.nil? %>
    <li>
  <% else %>
    <%= topbar_active_list(@title, @user.name) %>
  <% end %>
  <%= link_to t('link_profile'), current_user %></li>
  <%= topbar_active_list(@title, t(:title_user_edit)) %><%= link_to t('link_settings'), edit_user_path(current_user) %></li>
</ul>
